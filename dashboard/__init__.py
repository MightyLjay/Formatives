"""Local Streamlit dashboard for h2-totals-lab.

Runs on your machine (live odds + your local SQLite can't be reached by a hosted page). Pure data
logic lives in `dashboard.data` (no Streamlit import, unit-testable); the UI lives in `app.py`.
"""
